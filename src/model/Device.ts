export interface Device {
    name: string
    inputValue: number
    outputValue: number
    type: string
    seriesNumber: string
    status: string
    stateBattery: number
    maxDiscountAmout: number
    createdAt: string
}
